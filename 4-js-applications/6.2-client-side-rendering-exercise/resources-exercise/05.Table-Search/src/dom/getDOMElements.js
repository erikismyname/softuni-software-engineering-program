export function getDOMElements() {

    const tableBodyEl = document.querySelector('tbody');

    const inputEl = document.querySelector('input');

    const btnEl = document.querySelector('button');

    return { tableBodyEl, inputEl, btnEl };

}